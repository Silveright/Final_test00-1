<?xml version="1.0" encoding="UTF-8" ?><!--  SQL 맵퍼 파일은 xml이기 때문에 제일 먼저 xml 선언이 옵니다. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Groups">
	 <!-- 모임 생성  -->
    <insert id="insertGroup" parameterType="com.project.test.mybatis.mapper.GroupMapper">
        insert into group_info (userid, group_name, group_img, content, area_name, catename)
        values (#{userid}, #{group_name}, #{group_img}, #{content}, #{area_name}, #{catename})

    </insert>
</mapper>